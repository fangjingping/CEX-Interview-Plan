# 演示：合约交易链路集成

串联撮合 → 风控冻结 → Outbox → 消息分发 → 账本 → 仓位 → 定价 → 行情 → 持久化的最小闭环。

## 演示内容
- 撮合成交生成事件并写入 Outbox。
- Outbox 发布到分区日志，模拟消息总线。
- 幂等消费写账本并更新行情。
- 下单前预冻结保证金，成交后更新仓位并生成标记价。
- 仓位事件写入持久化快照，支持恢复。

## 运行
```bash
mvn -pl demo-trade-pipeline test
```
